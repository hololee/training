{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build code by bazel",
            "type": "shell",
            "command": "bazel build :debug -c dbg",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}